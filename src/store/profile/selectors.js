export const selEmail = (state) => state.profileSlice.email;
export const selToken = (state) => state.profileSlice.token;
export const selUsername = (state) => state.profileSlice.username;
export const selBio = (state) => state.profileSlice.bio;
export const selImage = (state) => state.profileSlice.image;
