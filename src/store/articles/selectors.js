export const selArticles = (state) => state.articlesSlice.articles;
export const selArticlesCount = (state) => state.articlesSlice.articlesCount;
export const selArticle = (state) => state.articlesSlice.article;
export const selArticleStored = (state) => state.articlesSlice.articleStored;
export const selArticleDeleted = (state) => state.articlesSlice.articleDeleted;
export const selCurrentPage = (state) => state.articlesSlice.currentPage;
