export const selMessage = (state) => state.messageSlice.message;
export const selFlagMessage = (state) => state.messageSlice.flagMessage;
export const selFlagModal = (state) => state.messageSlice.flagModal;
export const selResize = (state) => state.messageSlice.resize;
export const selErrorsServer = (state) => state.messageSlice.errorsServer;
